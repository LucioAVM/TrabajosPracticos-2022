/*
 * Usuario.c
 *
 *  Created on: 10 jun. 2022
 *      Author: UGIO
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int DatosUsuario(int AoM, int id, char nombre[50], char apellido[50],
float precio,
int tipoPasajero,
char codigoVuelo[4],
int estado,)
{
	int retorno;
	int menu;

	int validacionNombre;
	int validacionApellido;

	retorno = 0;

	//PIDE DATOS DEL PASAJERO AL USUARIO
	if(nombre != NULL && apellido != NULL && precio != NULL && tipoPasajero && )
	{
		do
		{
			//SUBMENU

			//un if para cada pedido de datos
			//val 1 = submenu
			//val 2 = AoM

			if((menu == 1 && AoM == 1) || AoM == 0)
			{
				 //NOMBRE
				validacionNombre = getStringLetras("\n\nIngresar nombre del pasajero:\t", nombre);
				if(validacionNombre == -1)
				{
					printf("\n\nERROR, no se pudo ingresar correctamente el nombre \n");
				}
			}

			if((menu == 2 && AoM == 1) || AoM == 0)
			{
				//APELLIDO
				validacionApellido = getStringLetras("\n\nIngresar apellido del pasajero:\t", apellido);
				if(validacionApellido == -1)
				{
					printf("\nERROR, no se pudo ingresar correctamente el apellido \n");
				}
			}

			if((menu == 3 && AoM == 1) || AoM == 0)
			{
				//PRECIO DE VUELO
				getFloat(&precio, "\n\nIngresar precio del vuelo:\t", "\nERROR, Ingresar precio del vuelo:\t",0,1000000, 5);
			}


			if((menu == 4 && AoM == 1) || AoM == 0)
			{
				//TIPO DE PASAJERO
			tipoPasajero = getInt("\n\nIngrese tipo de pasajero:\t","\nERROR, Ingrese tipo de pasajero:\t", 0,5,5);
			}

			if((menu == 5 && AoM == 1) || AoM == 0)
			{
				//CODIGO DE VUELO
				getString("\n\nIngrese codigo del vuelo: \t", codigoVuelo);
			}

			if((menu == 6 && AoM == 1) || AoM == 0)
			{
				//ESTADO DEL VUELO
				estado = getInt("\n\nIngrese estado del vuelo\t","\nERROR, Ingrese estado del vuelo\t", 0, 3, 5);
			}
		}while(menu != 7 || AoM == 0);//SE REPITE EL SUBMENU, ROMPE EN CASO DE SALIDA SUBMENU O AOM == 0
			//IF AOM ES 0 NO MUESTRA MENU Y OPCION DE SWITCH
		//SWITCH TIENE QUE SER UNA BANDERA

		//NO NIENE SWITCH

	}

	return retorno;
}
