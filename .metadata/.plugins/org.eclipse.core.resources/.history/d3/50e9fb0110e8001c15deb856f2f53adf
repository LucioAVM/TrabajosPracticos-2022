/*
 * Estado.c
 *
 *  Created on: 9 jun. 2022
 *      Author: UGIO
 */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "LinkedList.h"
#include "Pasajero.h"
#include "Estado.h"

eEstado iniciarisEmpty(void)
{
	eEstado estructuraEstado =
	{
			{1,"En Horario"},
			{2,"Aterrizado"},
			{3,"Demorado"}
	};

	return estructuraEstado;
}

int parsearEstadoPasajeroAEstructura(char estado[], ePasajero aux,eEstado estructuraEstado[])
{
	int retorno;

	retorno = 0;

	if(strcmp(estado,estructuraEstado[1].isEmptyChar))
	{
		aux.estado = estructuraEstado[1].isEmptyInt;
	}else{
		if(strcmp(estado,estructuraEstado[2].isEmptyChar))
		{
			aux.estado = estructuraEstado[2].isEmptyInt;
		}else{
			if(strcmp(estado,estructuraEstado[3].isEmptyChar))
			{
				aux.estado = estructuraEstado[3].isEmptyInt;
			}else{
				retorno = -1;
			}
		}
	}
	return retorno;
}


int parsearEstadoPasajeroDesdeEstrutura(char estado[], ePasajero aux,eEstado estructuraEstado)
{
	int retorno;

	retorno = 0;

	switch(aux.estado)
	{
	case 1:
		strcpy(estado,estructuraEstado[1].isEmptyChar);
		break;
	case 2:
		strcpy(estado,estructuraEstado[2].isEmptyChar);
		break;
	case 3:
		strcpy(estado,estructuraEstado[3].isEmptyChar);
		break;
	default:
		retorno = -1;
		break;
	}
	return retorno;
}
